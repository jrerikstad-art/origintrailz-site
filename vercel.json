{
  "buildCommand": "npm run build && node scripts/stage-www.mjs",
  "outputDirectory": "www",
  "installCommand": "npm install && npm --prefix hero ci",
  "framework": null,
  "rewrites": [
    { "source": "/map", "destination": "/map/index.html" }
  ]
}
